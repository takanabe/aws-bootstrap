require "spec_helper"

RSpec.describe Aws::Bootstrap do
  it "has a version number" do
    expect(Aws::Bootstrap::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
